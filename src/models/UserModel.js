class User {
	constructor({ id, firstName, lastName, email, dob }) {
		this.id = id;
		this.firstName = firstName;
		this.lastName = lastName;
		this.email = email;
		this.dob = dob;
	}
}

export default User;